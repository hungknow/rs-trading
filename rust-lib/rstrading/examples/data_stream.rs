/**
 * Receive the new tick from the source
 * Generate the new candle, then trigger the pipeline
 */

fn main() {
    // The candle is read from CSV file

    // The new tick is notified from the source
    // Generate the new candle from the latest stick
    // Notify the new candle

    // The TA data is generated for the new candle
}